# StockSense India — Architecture, Progress & Measured Truth (single source)

**Primary doc for the running system.** The v2 production upgrade is underway on branch `upgrade/product-v2` — its binding contract is `docs/upgrade-spec.md`, its evidence `docs/upgrade-audit.md`, its plan `docs/implementation-plan.md`, and its resumable state `docs/next-session.md`. Every claim below was measured live, not promised.

## v2 upgrade state (Risk-spec remediation T1–T4 complete, 2026-09-08)
- **One action authority**: decision-policy-v3 TradeGate — BUY_CANDIDATE requires fresh issuance + overlap-adjusted validated edge + positive Brier skill + calibration + dataQuality≥70 + forecastConfidence≥60 + entryQuality≥40 + EV-after-costs>0 + riskScore<80 (provisional thresholds, restrict-only). Everything else on screen is a labeled description; "conviction"/"BUY TODAY" vocabulary retired product-wide.
- **ScoreCard** (`decision/scorecard.ts`): setup/valuation/fundamental/businessQuality/entryQuality + unified riskScore + dataQualityScore (itemized penalties incl. unadjusted-corporate-action detector) + forecastConfidenceScore (measured OOS only — LOW system-wide today) + evidence-capped opportunity. Snapshots carry scoreCard/expectedValue/existingHolderAction/unmetGates.
- **Surfaces**: scan bucketed (BEST NEW ENTRIES gate-passed only — currently 0, stated), Stock Detail truth panel + Why-not-Buy + what-would-change, Track Record raw≈independent counts, scenario-frequency labels on all bootstrap numbers.
- **Integrity fixes**: partial today-bar no longer reaches callers; prediction_logs unique-indexed (deduped); research-brief probability substitution removed; measured-stats SQL pinned to model+14d recency; zero-volume/missing-prevClose no longer fabricate data; framework NO_DATA verdict.
- **Experiment registry live** (`experiments/runner.ts`, /api/experiments): champion vs constant-50/base-rate/always-up/zero-return on verified live rows; first run honestly SKIPPED all horizons (insufficient distinct dates). Block-bootstrap simulator EXPERIMENTAL, unwired.
- **AI committee** (Rule 13): InvestmentReasoningProvider + ClaudeProvider (ANTHROPIC_API_KEY; honest 503 absent), strict-JSON validated, CAP-ONLY in code, audit-trailed in ai_reviews.
- Full trail: docs/risk-spec.md → decision-audit.md → decision-remediation-plan.md → decision-report.md.

## v2 upgrade state (Phase C complete, 2026-09-06)
- **Session calendar** (`trading_sessions`): real sessions derived from observed stock_history bars; past weekdays with zero universe bars reconciled as closed (no_data); future weekdays are PROJECTED (provisional, no invented holiday list); reconciled at boot/evening cron.
- **Immutable forecasts** (`forecast_runs/points/outcomes`): append-only issuances (issuedAt, featureCutoffAt, anchor session+price, versions, sha256 input manifest); one per (instrument, anchor session) for next-30, revisioned per (instrument, month) for calendar-month snapshots vs latest-outlook refreshes. EVERY calendar day gets a point row; closed days carry NULL quantiles. Distribution = seeded per-step bootstrap MC (p05..p95 prices, genuine mean, pop). Outcomes graded SEPARATELY (verified/no_session/missing_data/pending; band-80/90 hits; revision-versioned corrections) — reality never rewrites a forecast.
- **Cadence**: evening cron reconciles calendar → grades outcomes → issues daily next-30 sweep → ensures monthly originals (idempotent, downtime-recovering). GETs read stored issuances only (B1 read/write contract); POST /api/forecast/:t/issue for on-demand.
- **API**: GET /api/forecast/:t/daily · GET /api/forecast/:t/month/:YYYY-MM (original vs latestOutlook vs actuals) · POST /api/forecast/:t/issue · POST /api/forecast/:t/month/:p/refresh · GET /api/holdings/projection (qty × price quantiles − basis; per-position only, quantiles never summed) · POST /api/jobs/forecast-maintenance.
- **UI**: Stock Detail → Forecast: day-wise table (every calendar day; "Market closed" states; 80%/90% intervals labeled exactly; outcome column) + issuance-boundary chart (observed solid, forecast median dashed + p10–p90 band, amber "issued" line). Holdings "Projected 30d" column now transforms the STORED issuance (no live recompute).

## v2 upgrade state (Phase B complete, 2026-09-06)
- **Migrations**: `synchronize:false` unconditionally; BaselineSchema + CreateAccountsInstrumentsWatchlistLedger applied; all schema change via reviewed migrations with tested down(). Scratch-DB build verified.
- **Money**: decimal.js policy (internal scale 4, display 2, ROUND_HALF_EVEN, largest-remainder allocation) in `src/services/money/`; spec §4 fixtures are unit + live-API verified (basis 1010 → realized +70.00 / 606.00 over 6 sh — exact).
- **Auth**: single seeded `owner` account (bcrypt, creds via .env OWNER_USER/OWNER_PASS), httpOnly SameSite=Strict session cookie + X-Requested-With CSRF header on mutations; all private routes scoped server-side; multi-account-ready schema.
- **Ledger/watchlist**: `accounts, instruments(+aliases, effective-dated), watchlist_items, ledger_transactions(immutable, corrections via corrects_id), lot_allocations(FIFO)`; watch ≠ own; oversell atomically rejected; positions derived, never editable.
- **Mutating reads FIXED**: GET /api/backtest is pure compute (upsert moved to POST /api/jobs/backtest w/ job log); GET /api/research no longer touches PredictionLog/Analysis (append-only-if-absent logging elsewhere).
- **REMOVED from product** (archive/ keeps code; DB records preserved): Sensei, portfolio allocation builder + daily plan, consumer Kelly (+drift, execution feedback), 6-model ensemble + FTRL from production, options skew, goal/milestone paths, standalone holdings calculator. Removed routes 404: /api/assistant, /api/position-size, /api/execution/summary, /api/admin/daily-plan, /api/portfolio/suggest, /api/holdings/calculate, /api/options/skew. Cron now: morning scan+rank-snapshot+intelligence rotation; evening verify; midnight cleanup.
- **Navigation**: Watchlist (default) / Holdings / Discover / Track Record + #stock/<t> drill-down + secondary Sandbox (old desk, records intact) & Diagnostics. Legacy hashes redirect.
- **Historical measurements** (direction ≈ coin flip, ~85% band coverage, Brier ≈ 0.2525) are HISTORICAL walk-forward results, not current production constants — see Track Record.

## Ground rules (bind every change)
Free data only (no paid keys). NEVER fabricate a number — missing = NOT_AVAILABLE, never 0. INR everywhere (en-IN). Vocabulary BUY/HOLD/AVOID. Every prediction carries an 80% range + measured accuracy. Honest degradation (`dataStatus`, reasons, caveats). Out-of-sample/walk-forward only — zero lookahead, never backfill a metric (e.g., rank IC). Provenance on computed metrics: formula + inputs + source + status + confidence.

## Run
- Backend: `npm run dev` → Express/TypeORM on **:5101** (Postgres DB `stock_analysis`, synchronize=true in dev).
- Frontend: `cd frontend && npx next dev -p 3001` → **:3001**. (**Port 3000 belongs to another project (dazz_nest) — never kill it.**)
- Jest: `npx jest` (12 suites / 178 tests green as of V10). Scripts: `scripts/refreshBacktests.ts` (re-backtest universe + model pool), `scripts/updateEnsemble.ts`, `scripts/recordKellyDrift.ts`, `scripts/persistRankSnapshot.ts`, `scripts/rotateIntelligence.ts` (on-demand run of the nightly rotating intelligence refresh), `scripts/seedAndBacktest.ts`, `scripts/testMarketData.ts`.

## Data sources (all verified live)
Yahoo (keyless): v8 chart (bars + quote meta; primary), v1 search (India-filtered; its news[] is currently a GENERIC feed → relevance-filtered), v10 quoteSummary via cookie+crumb (fundamentals), `^NSEI`, `^INDIAVIX`, `INR=X`. Google News RSS (IN) — main news source, 48h half-life decay. NSE XBRL corporate filings (Intelligence Engine only; cookie warm-up works for filings, but nseindia.com quote/option-chain APIs are **Akamai-blocked** — measured 403/empty). RBI published rates (live). MoSPI: adapter present, returns nothing (needs registration key). Universe: 151 live-verified NSE tickers in `src/data/nseUniverse.ts` (TATAMOTORS→TMCV/TMPV, Zomato→ETERNAL, LTIM→LTTS).

## Backend map (`src/`)
- `services/market/` — MarketDataService (bars DB-cached in stock_history, delete+insert upserts, partial-today-bar dropped pre-15:40 IST; resolve/search filter out mutual funds/ETFs + verify real history), FundamentalsService (crumb), MacroService (regime risk-on/neutral/risk-off), NewsService (2 sources, lexicon sentiment, hype = min(100, 8×fresh24h + 50×|sent|)), OptionsRadar (probe-first, NOT_AVAILABLE with evidence).
- `services/quant/` — engine (score 0-100, shrunk-drift+tilt predictions, 80% bands), backtest (walk-forward + Brier/probBuckets), montecarlo (seeded 10k bootstrap), models/ (6-model pool + inverse-Brier selector), volforecast (HAR-RV daily proxy + HAR-X with z-scored prev-day ^INDIAVIX; both fitted, walk-forward OOS winner forecasts, both R²s reported), rank (cross-sectional z: 50% momentum + 30% quality [stored-intelligence score when present, else quoteSummary ROE/op-margin — labeled per row] + 20% sentiment), intelligenceQuality (pure 0-100 over ROIC/FCF/FCF-margin/CR/PEG, bank-aware, renormalized, null <2 inputs), dcfPrior (pure b prior from stored DCF scenarios, clamp [0.5, 3.0]), kelly (f*=p−(1−p)/b, clamp 0.25, half-Kelly headline; b precedence measured > DCF-implied prior > structural 1.5), stress (historical NIFTY windows beta-mapped + block bootstrap).
- `services/framework/` — 8-phase owner framework (Master Score), entryTiming (BUY_TODAY ≥62 / WAIT 40-61 / AVOID_ENTRY; news-aware on Analyze), plan (2×ATR / 5-7.5% stops, 3:1 target), fees (Indian delivery model — ₹1k round trip ≈ 2%).
- `services/` — StockService (orchestrator: analyze/top-picks/backtest/accuracy/calibration/research-brief/chart/universe; 30-min scan cache, single-flight), PortfolioService (strategies short/long/balanced, regime cash reserve 100/80/50%, tranches, ρ=0.35 combined bands), admin/ (paper desk: trades FIFO, goals, loss guard 3% equity/day, prediction audit, ExecutionAnalytics: measured p last-30 ≥30 trades / b ≥10 both sides → adaptive Kelly + KellyDrift nightly), ensemble/ (FTRL regret weights), holdings/ (bought-at-X P&L), assistant/ (rule-based stocks-only Sensei), intelligence/ (see below), CronService (08:45 IST scan+log+rank-snapshot, then rotating intelligence refresh — 10 stalest universe tickers, never-stored first, <24h skipped, ≥5s throttle, per-ticker failures logged; 18:30 verify actuals + ModelPerformance + ensemble weights + Kelly drift; midnight cleanup — never deletes PredictionLog/ModelPerformance).
- Entities: Stock, StockHistory, Analysis, PredictionLog (predictions logged BEFORE outcomes; evening job fills actuals), ModelPerformance (per-ticker walk-forward stats + per-model briers, jsonb), EnsembleWeight, RankSnapshot, KellyDrift, PaperAccount/PaperTrade (the desk ledger IS the execution log; V10: nullable `entry_context` jsonb snapshots BUY-time quantScore/masterScore/entryTimingScore/intelligenceQuality/regime from already-computed values only, partial-sell splits inherit it), Intelligence{Source,Metric,Evidence}.

## API (all `{success,data}`; port 5101)
`/health` · `GET /api/search?q=` · `POST /api/analyze {ticker, amount?}` (quote, quant analysis, predictions+projections, framework, fundamentals, macro, news, monteCarlo, entryTiming, ensemble, beta1y, tradePlan, investmentPlan, accuracy, chart, disclaimer) · `GET /api/top-picks?count&maxPrice` · `GET /api/backtest/:ticker?days` (⚠ upserts ModelPerformance with that N — re-run refreshBacktests after small-N calls) · `GET /api/accuracy` (+perStockCaveat) · `GET /api/calibration` (Brier + reliability buckets + models aggregate + modelDrift) · `GET /api/research/:ticker` (deterministic brief) · `GET /api/rank/universe` (⚠ first cold call ~90s; cached after; V10: per-row qualitySource + componentStatus.quality `intelligence (n inputs)|quoteSummary fallback|no-data`, intelligenceCoverage {covered,total,note}) · `GET /api/volatility/forecast/:ticker` (V10: method HAR|HAR-X, r2OutOfSampleHar/HarX, vixDeltaR2, repoRatePct context-only) · `GET /api/options/skew/:ticker` (NOT_AVAILABLE, evidence-backed) · `GET /api/position-size/:ticker?capital` (V8 model + V9 measured + applied pair + comparison; V10: payoff.kind measured|dcf-implied|structural) · `GET /api/execution/summary` (V10: +factorInsights) · `POST /api/portfolio/suggest {amount, strategy}` · `GET /api/portfolio/stress|calibration?tickers=` · `POST /api/assistant {message}` · admin: `/api/admin/account(/settings)`, `/trades`, `/daily-plan`, `/prediction-audit` · intelligence: `/api/intelligence/:ticker(/refresh)`, `/macro(/refresh)`, `/portfolio`, `/source-registry`.

## Frontend (`frontend/src`, Next.js 16 + Tailwind 4 + Recharts + framer-motion)
Obsidian-aurora glass design, Space Grotesk display font, hash routing (#analyze/#top/#leaders/#portfolio/#accuracy/#stocks/#desk — survives refresh). Tabs: Analyze (verdict + entry timing + news radar + Monte Carlo cone + projections default ₹1,000 + holdings calc + framework + brief + vol card w/ V10 HAR-vs-HAR-X winner chip/both R²s/repo context + Kelly card w/ V10 payoff-source badge + prior explanation), Top 5 (budget filter), Leaders (rank + V10 quality-source chips + intelligence coverage hero/note), Portfolio (strategies + stress), Accuracy (measured tables + calibration/reliability + models), Stocks (entry column), Trading Desk (reality check, loss guard, plan+allocation, positions, prediction audit, performance feedback + Kelly drift + V10 factor insights w/ unlock note, execution log w/ filters), Sensei chat FAB.

## Measured truth (the numbers that matter)
| What | Measured |
|---|---|
| Direction hit rate (walk-forward, ~39k samples) | 50–51.5% — a coin flip, at every horizon |
| 80%-band coverage | ~85% (slightly conservative — the real product) |
| Brier vs 0.25 coin-flip | ≈0.2525 (~1% worse — probabilities honest, no edge) |
| V7 6-model ensemble vs single engine | +0.0002..+0.0054 Brier WORSE — no model beats coin flip (best cell macro 30d 0.2491) |
| HAR-RV out-of-sample R² (daily proxy, 10 tickers) | median +0.008 (range −0.09..+0.09) |
| V10 HAR-X (prev-day India VIX) vs plain HAR, same walk-forward days | per-ticker winner forecasts: RELIANCE HAR-X wins ΔR² +0.004 (0.0224 vs 0.0184); TCS Δ −0.0118 / TATASTEEL Δ −0.0512 → plain HAR wins there |
| V10 intelligence quality coverage (rank pillar) | 8/151 tickers scored from STORED NSE filings (15 stored, 5 with DCF scenarios); rest labeled quoteSummary fallback; rotating nightly refresh → ~3 weeks to full universe |
| Rank IC | null — collecting since 2026-09-01, unlocks ~7 weeks (never backfilled) |
| Kelly (no trades yet) | universe p 0.5049 + structural b 1.5 → half-Kelly 8.74% |
| V10 DCF-implied Kelly prior (pre-measurement) | TATASTEEL b=0.505 = (base 221.78−184.07)/\|109.39−184.07\| at ₹184.07; RELIANCE raw −0.6238 → clamped to 0.5 floor; measured b (proven live with 25 synthetic closed trades → b=2.0) always wins |
| Per-stock 15d/30d hit-rate extremes (e.g. 100%) | overlapping-window trend artifacts (~40 windows ≈ 2-3 independent obs) — dimmed + caveated in UI |
| Intelligence Engine (NSE XBRL, ~13s/ticker, 18 filings) | TCS ROE 48.97% / ROIC 53.8 / CR 2.23 / PEG 12.68(hist) — hand-checked; HDFCBANK bank-aware (ROIC/CR honestly N/A); TATASTEEL CR 0.75 FAIL; RBI repo 5.25% live |

## Version history (all verified)
V1 rebuild (Yahoo-only, backtester) → V2 framework+desk → V3 allocation/holdings/Sensei/UI → V4 routing/budget-picks/audit/loss-guard → V5 entry-timing/news-decay/Monte-Carlo → V6 Brier calibration + research brief → V7 ensemble (proved no direction edge) → V8 rank/HAR-RV/options-probe/Kelly → V9 execution feedback (measured p/b, adaptive Kelly, drift) → V10 intelligence bridge (verified 2026-09-02, section below).

## Declined honestly (and why)
Satellite/supply-chain, earnings-call audio NLP, dark-pool/UOA: no free Indian sources. HGNN/RL/TS-LLM: overfit theater at 151×2y scale. PC-algorithm causality on 60d: noise. User personalization: sample too small (single user). "100% accuracy"/"Brier<0.12"/"0 loss": mathematically dishonest — the app measures instead.

## Ops notes
Duplicate stale ts-node-dev watchers may race on :5101 after many restarts (kill extra terminals). MoSPI needs an API key (adapter ready). Order-book extraction needs a caller-supplied IR URL; BSE cross-check needs bseCode. Paper fills have zero slippage — live results will be worse by fees+slippage (stated in UI). Admin account is user state — never reset without confirmReset.
Bars freshness is trading-day-aware (2026-09-04): `lastCompletedTradingDate()` (IST weekday/15:40-close aware) gates `isCoverageFresh`, so daily data self-heals on read (RELIANCE last bar = live-quote close, verified); a holiday guard (`recentBarFetchAttempts` map, 30-min TTL) prevents refetch storms on days with no new bar.

## V10 — Intelligence-driven feedback bridge (verified 2026-09-02)
Bridges the Intelligence Engine's STORED NSE-filing metrics into rank, vol, Kelly and execution feedback. No new tables; never a bulk NSE fetch (~13s/ticker) — coverage builds via the rotating nightly refresh (10 stalest tickers after the 08:45 scan, ≥5s throttle → full universe in ~3 weeks).
- **Rank quality pillar**: pure `intelligenceQualityScore` (0-100 over ROIC≥15, FCF>0, graded FCF-margin/20%, CR≥1 banks-excluded, 0<PEG<1.5; weights renormalized over available inputs, null under 2) replaces the quoteSummary fallback per ticker when stored metrics exist. Live: **8/151 covered** (15 tickers stored; e.g. TCS 5 inputs qZ +2.3, HDFCBANK 2 inputs bank-aware, TATASTEEL 5 inputs), 143 rows labeled `quoteSummary fallback`, response carries intelligenceCoverage + honest coverage note.
- **HAR-X vol**: fits plain HAR AND HAR-X (z-scored previous-day ^INDIAVIX, z params from the train window only; VIX bars memory-cached, never DB), compares walk-forward OOS on the SAME days, the winner forecasts. Measured: RELIANCE HAR-X wins ΔR² **+0.004** (0.0224 vs 0.0184); TCS Δ −0.0118 and TATASTEEL Δ −0.0512 → plain HAR wins there. Repo rate 5.25% is a context chip only — stated in the note as never-a-regressor (rare step events, unfitable on ~250 obs).
- **DCF-implied Kelly prior**: with <10 closed both-sides trades AND stored DCF scenarios, b = (base-or-bull upside)/|bear downside| vs current price, clamped [0.5, 3.0], source "DCF-implied (assumption-sensitive — bear/base/bull scenarios, not measured)". Hand-checked live: TATASTEEL b=0.505 = (221.78−184.07)/|109.39−184.07| at ₹184.07 (raw 0.5050); RELIANCE raw −0.6238 → clamped to the 0.5 floor, clamp stated in the comparison string. Precedence measured > dcf-implied > structural proven live: 25 tagged synthetic closed trades flipped TATASTEEL to measured b=2.0; deleting them restored dcf-implied 0.505 with paper tables byte-identical (sha256-matched pg_dump before/after).
- **Entry-context factor insights**: `PaperTrade.entry_context` snapshots BUY-time beliefs from already-computed values (zero extra network; masterScore honestly null). `/api/execution/summary.factorInsights` splits win rate by factor at ≥20 closed-with-context, emitting only splits with both sides n≥8 — hand-verified on synthetic rows (quantScore≥60: 75.0% vs 33.33%, Δ41.67pp; a 7-trade side correctly suppressed; per-factor null exclusion). Currently locked, honestly: "insights unlock at 20 closed trades with entry snapshots — currently 0".
- **Frontend** (headless-CDP verified, zero console errors): Leaders quality-source chips + `intelligence 8/151` hero + coverage note; vol card winner chip + both OOS R²s + India VIX ΔR² + repo context chip; Kelly payoff-source badge (measured emerald / DCF-implied violet / structural zinc) + one-line prior explanation; Desk "Execution insights by factor" block with the unlock note. All V10 chrome hides against pre-V10 responses.

## UI design system (V11 — frontend redesign, audited 2026-09-02)
- **Tokens**: `frontend/src/app/globals.css` `:root` is the CSS contract, mirrored 1:1 in `frontend/src/lib/design-tokens.ts` (COLOR/GLASS/ELEVATION/SPACE/FONT_SIZE/FONT_FAMILY/RADIUS/DURATION/EASE/KEYFRAMES/Z_INDEX). Base `--bg-base` #0A0A0F obsidian + CSS-only aurora (mesh/blobs/3% SVG grain); ink slate-50/400/500; accents BUY `--accent-buy` #00D4AA · SELL #FF4D6D · HOLD/WAIT amber (utilities text-buy/bg-buy/10 etc.). Fonts via next/font: Inter (body), Space Grotesk (display), Geist Mono; `tabular-nums` global. `prefers-reduced-motion` pauses ambient layers, disables decorative animation (spinners kept), framer via app-wide `MotionConfig reducedMotion="user"`; page-level `overflow-x: clip` guarantees no horizontal scrollbar.
- **Component library**: `frontend/src/components/ui/` (19 files + `index.ts` barrel re-exporting the full legacy API — GlassCard, Button, Chip, Input/SearchInput, Select, DataTable, Modal/Drawer, Tooltip/InfoTip, Tabs, meters, ChartFrame, Collapsible, AnimatedNumber, Sparkline, StatTile, ViewHero, skeletons, empty/error states). Gallery: **`/ui-showcase`** — every component in data/loading/empty/error states, all sample numbers labeled fabricated.
- **Measured audit** (headless Chrome CDP vs :3001 dev + `next start` prod; **Chrome-only — no Firefox/Safari on this machine**): all 7 tabs + /ui-showcase × 4 viewports (1440/1024/768/375) = zero console errors, zero page horizontal overflow, honesty markers (reality check, disclaimers, coin-flip/Brier caveats, NOT_AVAILABLE, measured-accuracy strips) present on every page. Audit fixes shipped: rotating `.hero-ring` bbox gave every tab a 375px scrollbar (now spins inside a circle-clipped shell), breakpoint-only grids lacked base `grid-cols-1` (min-content blowouts on #accuracy 520px / showcase 711px), ViewHero chip row `shrink-0` overflow (leaders 479px), "Record all N BUYs" white-on-emerald 3.65:1 → buy-token surface, BandDot spans `role="img"`, desk skeletons resized to measured real heights (CLS 0.251 → 0.089). Interactions verified: tab switching, collapsible, drawer+modal Escape/focus-restore/scroll-lock, Select listbox, aria-sort cycling, tooltips on hover+focus, Record-BUY reachable ≥44px, chat FAB.
- **Lighthouse 12** (prod `next start`, mobile defaults; #analyze scores its empty search state — hash routes can't carry a ticker into a fresh session): **#analyze perf 86 / a11y 100 / BP 100** (FCP 1.1s, LCP 4.2s, TBT 70ms, CLS 0); **#desk perf 76 / a11y 100 / BP 100** (LCP 5.2s data-fetch-bound, TBT 180ms, CLS 0.089). Remaining perf flags are serving-infra (`next start` no text compression, unused-JS) not UI. Scroll jank (CDP trace, 1440px, loaded Analyze, 7.5s scroll): **3 dropped / 454 frames ≈ 0.66%**, zero >50ms main-thread tasks — headless on an idle machine; real devices will be worse.

## Forecast Lock — purchase-day predictions vs reality (verified 2026-09-04)
Every desk BUY freezes that day's 7d/30d forecast into `PaperTrade.entry_context.forecastLock` (anchor `baseClose` + expected/low/high + targetDate, from the buy-path analysis — NEVER recomputed). `GET /api/admin/forecast-locks` → {rows, note, updatedAt}: tracks live price vs the frozen band and grades matured horizons against the REAL close on/before the target date (withinBand / directionHit / errorPct in pp vs the anchor close; position P&L reported separately; pre-feature trades honestly absent). Desk UI `components/admin/ForecastLockCard.tsx` (Collapsible after Performance Feedback, 404-hides): 30d band rail low→expected→high with buy/sell live marker vs anchor, "day X of 30 · target date" or MATURED chip, WITHIN/OUTSIDE BAND + DIRECTION HIT/MISS verdict chips, backend note as caption. Verified live with a real SAIL BUY (lock base 196.80; matured verdicts hand-checked: actual 197.00 → +0.10%, error 0.99 pp @7d / 2.65 pp @30d) then deleted with sha256-identical paper tables; same pass fixed StressTestCard's `method` string rendering as a nowrap chip (~1,558px overflow once a position existed) — now a wrapping caption.
